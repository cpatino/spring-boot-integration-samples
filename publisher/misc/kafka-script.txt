# Open the docker container, and run the following command to create a topic named "my-topic" with the Kafka CLI tool:
cd /opt/kafka/bin
./kafka-topics.sh --create --topic my-topic --bootstrap-server 127.0.0.1:9092
